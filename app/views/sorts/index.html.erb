<h1>Listing sorts</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sorts.each do |sort| %>
  <tr>
    <td><%= sort.name %></td>
    <td><%= link_to 'Show', sort %></td>
    <td><%= link_to 'Edit', edit_sort_path(sort) %></td>
    <td><%= link_to 'Destroy', sort, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sort', new_sort_path %>
<%= link_to 'Magage articles', articles_path%>
